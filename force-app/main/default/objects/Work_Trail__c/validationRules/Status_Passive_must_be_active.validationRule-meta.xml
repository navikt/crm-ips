<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Status_Passive_must_be_active</fullName>
    <active>true</active>
    <description>Participant cannot be set to &quot;Referred&quot; or &quot;Ended&quot; while registered as passive. The participant must be active (not passive) to change status to these values.</description>
    <errorConditionFormula>AND(
  OR(
    ISPICKVAL(ips_Status__c, &quot;Referred&quot;),
    ISPICKVAL(ips_Status__c, &quot;Ended&quot;)
  ),
  passive_status__c
)</errorConditionFormula>
    <errorDisplayField>ips_Status__c</errorDisplayField>
    <errorMessage>The participant is registered as passive. Remove the passive check mark before setting the status to &quot;Referred&quot; or &quot;Ended&quot;.</errorMessage>
</ValidationRule>
